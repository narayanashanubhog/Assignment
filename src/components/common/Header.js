import React,{PropTypes} from 'react';
import {Link,IndexLink} from 'react-router';

const Header = () =>{
    return (
        <nav>
            <h1 className="header">
                <br />
            </h1>
        </nav>
    );
};
export default Header;